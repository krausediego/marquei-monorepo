info:
  name: getApiV1Hello-worldByMessage
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api/v1/hello-world/:message"
  params:
    - name: message
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Response for status 200
    request:
      url: "{{baseUrl}}/api/v1/hello-world/:message"
      method: GET
      params:
        - name: message
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "message": ""
          }
