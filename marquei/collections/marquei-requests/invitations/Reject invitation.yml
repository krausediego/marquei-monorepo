info:
  name: Reject invitation
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseURL}}{{prefix}}/invitation/reject"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
