info:
  name: List invitations
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseURL}}{{prefix}}/invitation"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
