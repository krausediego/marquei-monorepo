info:
  name: Sign in email
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseURL}}{{prefix}}/auth/sign-in/email"
  body:
    type: json
    data: |-
      {
        "email": "ana.babiela@gmail.com",
        "password": "diego0123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
