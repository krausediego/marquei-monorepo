info:
  name: Sign up email
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseURL}}{{prefix}}/auth/sign-up/email"
  body:
    type: json
    data: |-
      {
        "name": "Ana gabriela",
        "email": "ana.babiela@gmail.com",
        "password": "diego0123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
