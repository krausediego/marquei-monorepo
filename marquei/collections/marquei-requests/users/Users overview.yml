info:
  name: Users overview
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseURL}}{{prefix}}/user/overview"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
