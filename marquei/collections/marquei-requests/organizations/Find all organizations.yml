info:
  name: Find all organizations
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseURL}}{{prefix}}/organization"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
