info:
  name: Accept invitation
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseURL}}{{prefix}}/invitation/accept"
  body:
    type: json
    data: |-
      {
        "invitationId": "019ca4e5-9f0c-7000-92cc-23b95f4ff161"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
