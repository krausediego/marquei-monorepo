info:
  name: getApiV1Hello-world
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/v1/hello-world/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
