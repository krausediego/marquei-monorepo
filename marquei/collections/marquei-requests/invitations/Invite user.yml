info:
  name: Invite user
  type: http
  seq: 4

http:
  method: POST
  url: "{{baseURL}}{{prefix}}/invitation"
  body:
    type: json
    data: |-
      {
        "email": "ana.babiela@gmail.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
